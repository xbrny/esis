<% if obj.errors.any? %>
<div class="col-xs-offset-2 col-xs-10 no-padding">
  <div class="panel panel-danger">
    <div class="panel-heading">
      <p><strong><%= pluralize(obj.errors.count, "error") %> prohibited this subject from being saved:</strong></p>
      <ul>
        <% obj.errors.full_messages.each do |message| %>
        <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  </div>
</div>
<% end %>
