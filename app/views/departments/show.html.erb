<h1 class="page-header text-center"><%= @department.name %></h1>

<%= link_to 'Delete', department_path(@department), method: :delete,
              data: { confirm: "Are your sure you want to delete this department?" },
              class: "btn btn-lg btn-danger btn-block" %>

<%= link_to 'Edit', edit_department_path(@department), class: "btn btn-lg btn-default btn-block" %>
